# 14 RPC 服务使用 gorm

本目录用于完成第 14 章：在 RPC 服务中引入数据访问层（gorm），完成一次端到端的数据读写闭环。

## 你将学到

- gorm 在 RPC 场景下的数据访问落点
- 数据访问错误的处理与返回策略
- RPC 方法与数据层的解耦思路

## 待办

1. 在 RPC 侧实现一个“查询/写入”用例
2. 使用 gorm 完成数据访问并返回结果
3. 验证异常场景（例如无数据/写入失败）的返回是否符合预期

## 运行与验证

- 客户端调用 RPC 方法，确认返回数据正确

## 参考

- 待补充

