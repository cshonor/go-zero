# 08 jwt 验证失败的响应

本目录用于完成第 8 章：当 JWT 校验失败或 token 无效时，返回统一、清晰且可被前端处理的错误响应。

## 你将学到

- 常见失败场景（过期、签名错误、格式错误、缺失 token）
- HTTP 状态码与业务错误码的映射策略
- 统一错误响应的实现方式

## 待办

1. 准备至少 3 类失败场景（缺失/错误/过期）
2. 确认返回结构与字段含义一致
3. 记录每种场景的请求示例与返回示例

## 运行与验证

- 构造失败请求，确认返回符合你定义的规范

## 参考

- 待补充

